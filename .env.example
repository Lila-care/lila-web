VITE_API_URL=
VITE_AUTH_DOMAIN=
VITE_CLIENT_ID=
VITE_COGNITO_CLIENT_ID=
VITE_REDIRECT_URI=

# Wompi Widget Checkout public key — empty until real sandbox/production keys exist. Without
# it, `useWompiCheckout` shows "Los pagos todavía no están configurados" instead of opening
# the widget (see src/Chat/useWompiCheckout.ts).
VITE_WOMPI_PUBLIC_KEY=
